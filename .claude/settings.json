{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) cd \"$CLAUDE_PROJECT_DIR\" && uvx ruff@0.16.5 format \"$f\" && uvx ruff@0.16.5 check --fix \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "ruff format + fix"
          }
        ]
      }
    ]
  }
}
